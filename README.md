# AULA-13  JAVA SCRIPT E SUAS VARIÁVEIS.
# ⚖️ Calculadora de IMC Simples

Um projeto simples em JavaScript para calcular o Índice de Massa Corporal (IMC) e classificar o resultado com base nos padrões de saúde. Ideal para demonstrar conceitos básicos de variáveis, operadores e estruturas condicionais (`if`/`else if`).

## 📸 Demonstração do Projeto

**[Substitua esta linha pela tag Markdown da sua imagem (Ex: `![Screenshot da Calculadora de IMC](caminho/para/sua/imagem.png)`)]**

> **Sugestão de Imagem:** Um screenshot do console do navegador ou de um ambiente de execução (como o Node.js) mostrando a saída do cálculo final do IMC.

## ✨ Funcionalidades

* **Cálculo de IMC:** Realiza o cálculo da fórmula: $IMC = \frac{peso}{(altura \times altura)}$.
* **Classificação:** Classifica o resultado em diferentes faixas (Abaixo do Peso, Peso Normal, Sobrepeso, etc.).
* **Interação com o Usuário:** Utiliza a função `prompt()` para coletar os dados de entrada (peso e altura).

## 🚀 Como Usar

Este projeto é um script simples que deve ser executado em um ambiente JavaScript:

1.  **Clone o Repositório:**
    ```bash
  # ⚖️ Calculadora de IMC Simples

Um projeto simples em JavaScript para calcular o Índice de Massa Corporal (IMC) e classificar o resultado com base nos padrões de saúde. Ideal para demonstrar conceitos básicos de variáveis, operadores e estruturas condicionais (`if`/`else if`).

## 📸 Demonstração do Projeto

**[Substitua esta linha pela tag Markdown da sua imagem (Ex: `![Screenshot da Calculadora de IMC](caminho/para/sua/imagem.png)`)]**

> **Sugestão de Imagem:** Um screenshot do console do navegador ou de um ambiente de execução (como o Node.js) mostrando a saída do cálculo final do IMC.

## ✨ Funcionalidades

* **Cálculo de IMC:** Realiza o cálculo da fórmula: $IMC = \frac{peso}{(altura \times altura)}$.
* **Classificação:** Classifica o resultado em diferentes faixas (Abaixo do Peso, Peso Normal, Sobrepeso, etc.).
* **Interação com o Usuário:** Utiliza a função `prompt()` para coletar os dados de entrada (peso e altura).

## 🚀 Como Usar

Este projeto é um script simples que deve ser executado em um ambiente JavaScript:

1.  **Clone o Repositório:**
    ```bash
    # ⚖️ Calculadora de IMC Simples

Um projeto simples em JavaScript para calcular o Índice de Massa Corporal (IMC) e classificar o resultado com base nos padrões de saúde. Ideal para demonstrar conceitos básicos de variáveis, operadores e estruturas condicionais (`if`/`else if`).

## 📸 Demonstração do Projeto

**[Substitua esta linha pela tag Markdown da sua imagem (Ex: `![Screenshot da Calculadora de IMC](caminho/para/sua/imagem.png)`)]**

> **Sugestão de Imagem:** Um screenshot do console do navegador ou de um ambiente de execução (como o Node.js) mostrando a saída do cálculo final do IMC.

## ✨ Funcionalidades

* **Cálculo de IMC:** Realiza o cálculo da fórmula: $IMC = \frac{peso}{(altura \times altura)}$.
* **Classificação:** Classifica o resultado em diferentes faixas (Abaixo do Peso, Peso Normal, Sobrepeso, etc.).
* **Interação com o Usuário:** Utiliza a função `prompt()` para coletar os dados de entrada (peso e altura).

## 🚀 Como Usar

Este projeto é um script simples que deve ser executado em um ambiente JavaScript:

1.  **Clone o Repositório:**
    ```bash
    git clone [LINK_DO_SEU_REPOSITÓRIO]
    ```
2.  **Execute o Arquivo:**
    * **No Navegador (Recomendado):** Inclua o código JavaScript em um arquivo HTML usando a tag `<script src="seu_arquivo.js"></script>` e abra o HTML no seu navegador. As caixas de diálogo (`prompt`) aparecerão.
    * **Com Node.js:** Se você tiver o Node.js instalado, pode executar o arquivo diretamente no terminal:
        ```bash
        node seu_arquivo.js
        ```

## 🛠️ Tecnologias Utilizadas

* **JavaScript (ES5+):** Para toda a lógica de cálculo e interação.
    * Métodos: `prompt()`, `Number()`, `console.log()`
    * Estruturas: `if`, `else if`

## 👨‍💻 O Código em Destaque

O coração do projeto está na lógica condicional para a classificação do IMC:

```javascript
// O cálculo
let imc = peso / (altura * altura);

// A classificação
if (imc < 18.5) {
   console.log("Abaixo do peso: " + imc)
} else if (imc < 24.9) { 
    console.log("Peso normal")
// ... (continua para as outras classificações)
}
    ```
2.  **Execute o Arquivo:**
    * **No Navegador (Recomendado):** Inclua o código JavaScript em um arquivo HTML usando a tag `<script src="seu_arquivo.js"></script>` e abra o HTML no seu navegador. As caixas de diálogo (`prompt`) aparecerão.
    * **Com Node.js:** Se você tiver o Node.js instalado, pode executar o arquivo diretamente no terminal:
        ```bash
        node seu_arquivo.js
        ```

## 🛠️ Tecnologias Utilizadas

* **JavaScript (ES5+):** Para toda a lógica de cálculo e interação.
    * Métodos: `prompt()`, `Number()`, `console.log()`
    * Estruturas: `if`, `else if`

## 👨‍💻 O Código em Destaque

O coração do projeto está na lógica condicional para a classificação do IMC:

```javascript
// O cálculo
let imc = peso / (altura * altura);

// A classificação
if (imc < 18.5) {
   console.log("Abaixo do peso: " + imc)
} else if (imc < 24.9) { 
    console.log("Peso normal")
// ... (continua para as outras classificações)
}
    ```
2.  **Execute o Arquivo:**
    * **No Navegador (Recomendado):** Inclua o código JavaScript em um arquivo HTML usando a tag `<script src="seu_arquivo.js"></script>` e abra o HTML no seu navegador. As caixas de diálogo (`prompt`) aparecerão.
    * **Com Node.js:** Se você tiver o Node.js instalado, pode executar o arquivo diretamente no terminal:
        ```bash
        node seu_arquivo.js
        ```

## 🛠️ Tecnologias Utilizadas

* **JavaScript (ES5+):** Para toda a lógica de cálculo e interação.
    * Métodos: `prompt()`, `Number()`, `console.log()`
    * Estruturas: `if`, `else if`

## 👨‍💻 O Código em Destaque

O coração do projeto está na lógica condicional para a classificação do IMC:

```javascript
// O cálculo
let imc = peso / (altura * altura);

// A classificação
if (imc < 18.5) {
   console.log("Abaixo do peso: " + imc)
} else if (imc < 24.9) { 
    console.log("Peso normal")
// ... (continua para as outras classificações)
}
